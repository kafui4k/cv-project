import styled from 'styled-components';

export const InputWrapper = styled.input`
  padding: 0.5em;
  border: none;
  border-radius: 3px;
  background: lightgray;
  color: dark;
  font-size: 1em;
`;
